# Wine Quality Classification

This project implements multiple machine learning models (Logistic Regression, Decision Tree, KNN, Naive Bayes, Random Forest, XGBoost) to classify wine quality using the UCI Wine Quality dataset.

## 📂 Structure
- `app.py` / `streamlit_app.py`: Streamlit app for demo
- `requirements.txt`: Dependencies
- `README.md`: Documentation
- `model/`: Jupyter notebook and saved models

##  How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/classification-ml-model.git
   cd classification-ml-model
